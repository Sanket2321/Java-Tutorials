package com.company;
import java.util.Scanner;

public class J05_Arrays {
    public static void main(String[] args) {

        /*
        Scanner sc = new Scanner(System.in);

        //declaration and then memory allocation and then initialization
        int[] numbs;
        numbs = new int[5];
        numbs[0] = 5;
        numbs[1] = 57;
        numbs[2] = 89;


        //declaration and memory allocation
        int[] numbs = new int[5];


        //declaration, memory allocation, initialization all together
        int[] numbs = {5,8,44,695,785,41};


        //printing using for loop
        int[] age = {18,45,74,69,54,26,20};
        for(int i=0; i<age.length;i++){
            System.out.println(age[i]);
        }


        //printing using for-each loop
        String[] cars = {"bmw","mercedes","audi","volvo","lexus"};
        for(String i : cars){
            System.out.println(i);
        }

        int[] nums = {1,2,3,4,5};
        for(int j : nums){
            System.out.println(j);
        }

        float[] float_nums = {12.8f,7.5f,96,4f};
        for(float k : float_nums){
            System.out.println(k);
        }


        //multidimensional array
        int[][] flat = new int[2][3];
        flat[0][0] = 101;
        flat[0][1] = 102;
        flat[0][2] = 103;
        flat[1][0] = 201;
        flat[1][1] = 202;
        flat[1][2] = 203;
        System.out.println(flat.length);
        System.out.println(flat[0].length);
        for (int i=0;i<flat.length;i++){
            for(int j=0;j<flat[i].length;j++){
                System.out.print(flat[i][j] + " ");
            }
            System.out.println();
        }


        //above same can be written as
        int[][] flat1 = {{101,102,103},{201,202,203}};
        for(int i=0;i<flat1.length;i++){
            for(int j=0;j<flat1[i].length;j++){
                System.out.print(flat1[i][j] + " ");
            }
            System.out.println();
        }


        //Problems
        //reverse an array
        int[] arr = {5,8,9,4,3,7};
        int len = arr.length;
        int n = Math.floorDiv(len,2);


        //swapping ith element with the l-i-1 th element
        for(int i=0;i<n;i++){
            int temp = arr[i];
            arr[i] = arr[len-1-i];
            arr[len-1-i] = temp;
        }
        for (int element:arr){
            System.out.print(element + " ");
        }


        //find maximum in a array
        int[] arr = {8,7,3,9,1,2};
        int maxElement = Integer.MIN_VALUE;
        for (int i:arr){
            if(i>maxElement){
                maxElement = i;
            }
        }
        System.out.println(maxElement);


        //find minimum in a array
        int[] arr = {8,7,3,9,1,2};
        int minElement = Integer.MAX_VALUE;
        for (int i:arr){
            if(i<minElement){
                minElement = i;
            }
        }
        System.out.println(minElement);


        //to check if array is sorted or not
        int[] arr = {1,3,2};
        boolean isSorted = false;
        for (int i=0;i<arr.length-1;i++){
            if(arr[i]<arr[i+1]){
                isSorted = true;
            }else{
                isSorted = false;
                break;
            }
        }
        if (isSorted==true){
            System.out.println("is sorted");
        }else{
            System.out.println("not sorted");
        }

         */

    }
}
